import 'package:flutter/material.dart';

class HP_motorKasar extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Motor Kasar'),
          centerTitle: true,
          backgroundColor: Color(0XFF40E0D0),
        ),
      );
}
