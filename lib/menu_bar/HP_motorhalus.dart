import 'package:flutter/material.dart';

class HP_motorhalus extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Motor Halus dan Pra-Menulis'),
          centerTitle: true,
          backgroundColor: Color(0XFF40E0D0),
        ),
      );
}
