import 'package:flutter/material.dart';

class perkembanganSosial extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Perkembangan Sosial'),
          centerTitle: true,
          backgroundColor: Color(0XFF40E0D0),
        ),
      );
}
